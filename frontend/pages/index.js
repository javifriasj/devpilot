export default function Home() {
  return (
    <main style={{padding:40,fontFamily:'sans-serif'}}>
      <h1>devpilot — Next.js frontend</h1>
      <p>Frontend mínimo. Prueba el backend en <code>/api/health</code> (desde el servidor FastAPI).</p>
      <p>
        <a href="/about">About</a>
      </p>
    </main>
  )
}
